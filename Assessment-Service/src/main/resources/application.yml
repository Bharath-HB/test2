server:
  port: 8083

spring:
  application:
    name: assessmentservice
  datasource:
    url: jdbc:mysql://localhost:3306/ita
    username: root
    password: pass@word1
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true